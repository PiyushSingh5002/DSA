#include <bits/stdc++.h>
using namespace std;

int solve(int n, int k, string &s) {
    
    return 0; 
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, k;
        cin >> n >> k;
        string s;
        cin >> s;
        cout << solve(n, k, s) << "\n";
    }
    return 0;
}